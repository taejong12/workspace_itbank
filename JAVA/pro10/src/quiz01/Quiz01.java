package quiz01;

public class Quiz01 {
	
	/*
	 * User 클래스 - id, pwd, name, email, joinDate - joinDate 의 date 타입은 java.sql 입니다.
	 * 
	 * UserService 클래스 - 회원가입, 회원삭제, 회원검색, 회원정보수정 - 회원 검색은 id 검색과 name 검색 2가지 해 볼것.
	 * - 회원정보수정은 id 를 제외하고 수정
	 * 
	 * UserMain - UserService 실행 클래스
	 * 
	 * Table CREATE TABLE t_user ( id varchar2(50) not null primary key, pwd
	 * varchar2(50) not null, name varchar2(50) not null, email varchar2(50) not
	 * null, joinDate date default sysdate );
	 * 
	 */

}
