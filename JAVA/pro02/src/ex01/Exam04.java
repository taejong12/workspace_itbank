package ex01;

public class Exam04 {
	public static void main(String[] args) {
		// 변수의 위치에 따라 {} 안에서만 사용 가능
		int val1; // main 메서드 내에서 사용 가능
		
		/*
		 * if() { int val2; // if 문 블럭안에서만 사용 가능 }
		 * 
		 * for() { int val3; // for 문 블럭안에서만 사용 가능 }
		 */
		
		// val1 사용 가능
		// val2 사용 불가능
		// val3 사용 불가능
	}
}